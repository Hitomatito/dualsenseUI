name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  deb:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Build .deb
        run: |
          set -ex
          VERSION=${GITHUB_REF_NAME#v}
          PKG=dualsense-ui_${VERSION}-1_all
          DEST=/tmp/$PKG
          mkdir -p $DEST/DEBIAN
          python3 -m pip install --root=$DEST --prefix=/usr .
          install -Dm644 data/com.dualsenseui.app.desktop $DEST/usr/share/applications/com.dualsenseui.app.desktop
          install -Dm644 data/icons/com.dualsenseui.svg $DEST/usr/share/icons/hicolor/scalable/apps/com.dualsenseui.svg
          install -Dm644 data/icons/usb-symbolic.svg $DEST/usr/share/icons/hicolor/symbolic/devices/usb-symbolic.svg
          cat > $DEST/DEBIAN/control <<EOF
          Package: dualsense-ui
          Version: ${VERSION}-1
          Architecture: all
          Maintainer: DualSense UI Contributors
          Depends: python3, python3-gi, gir1.2-gtk-4.0, python3-gi-cairo, dualsensectl, python3-hidapi, python3-evdev
          Section: utils
          Priority: optional
          Description: Graphical interface for PlayStation DualSense controllers
           DualSense UI provides a graphical interface for configuring and
           monitoring PlayStation DualSense and DualSense Edge controllers.
          EOF
          dpkg-deb --build --root-owner-group $DEST
          cp /tmp/${PKG}.deb .
      - uses: actions/upload-artifact@v4
        with:
          name: deb-packages
          path: dualsense-ui_*.deb

  rpm:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Install deps
        run: |
          sudo apt-get update
          sudo apt-get install -y rpm python3-setuptools python3-evdev
      - name: Build .rpm
        run: |
          set -ex
          VERSION=${GITHUB_REF_NAME#v}
          tar czf /tmp/dualsense-ui-${VERSION}.tar.gz \
            --transform "s,^,dualsense-ui-${VERSION}/," \
            --exclude=.git --exclude=.github --exclude=.pytest_cache --exclude=__pycache__ \
            -C $(pwd) .
          mkdir -p rpmbuild/{BUILD,RPMS,SOURCES,SPECS,SRPMS}
          cp /tmp/dualsense-ui-${VERSION}.tar.gz rpmbuild/SOURCES/
          rpmbuild -ba dualsense-ui.spec \
            --define "_topdir $(pwd)/rpmbuild" \
            --define "version ${VERSION}" > /tmp/rpmbuild.log 2>&1; rc=$?
          tail -50 /tmp/rpmbuild.log
          find rpmbuild -name '*.rpm' >> /tmp/rpmbuild.log
          exit $rc
      - if: always()
        uses: actions/upload-artifact@v4
        with:
          name: rpm-logs
          path: /tmp/rpmbuild.log
      - uses: actions/upload-artifact@v4
        with:
          name: rpm-packages
          path: rpmbuild/RPMS/**/*.rpm

  flatpak:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - name: Build Flatpak
        run: |
          set -ex
          sudo apt-get update
          sudo apt-get install -y flatpak flatpak-builder
          flatpak remote-add --user --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
          flatpak-builder --user --force-clean --install-deps-from=flathub build-dir flatpak/com.dualsenseui.yml 2>&1 | tee /tmp/flatpak.log
          flatpak build-bundle build-dir dualsense-ui.flatpak com.dualsenseui.app >> /tmp/flatpak.log 2>&1
      - if: always()
        uses: actions/upload-artifact@v4
        with:
          name: flatpak-logs
          path: /tmp/flatpak.log
      - uses: actions/upload-artifact@v4
        with:
          name: flatpak
          path: dualsense-ui.flatpak

  release:
    needs: [deb, rpm, flatpak]
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
      - uses: softprops/action-gh-release@v2
        with:
          files: |
            deb-packages/*.deb
            rpm-packages/*.rpm
            flatpak/dualsense-ui.flatpak
          generate_release_notes: true
