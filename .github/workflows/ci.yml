name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install system deps
        run: |
          sudo apt-get update
          sudo apt-get install -y libgirepository1.0-dev libhidapi-dev

      - name: Install Python deps
        run: |
          pip install pytest pytest-mock
          pip install pygobject hidapi evdev

      - name: Run tests
        run: |
          python3 -m pytest tests/ -v --tb=short
